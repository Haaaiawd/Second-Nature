# Wave 68 Review — Runtime-Ops v7 Plugin & Manual Run

> **Date**: 2026-05-23
> **Branch**: `feature/v7-wave61-dqs-c3`
> **Wave**: 68
> **Scope**: T-ROS.C.2 (Plugin Registration), T-ROS.C.3 (ManualRunDispatcher), T-ROS.C.4 (README/AGENTS.md), T-ROS.C.5 (v6 Regression Gate)

---

## 1. 变更清单

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `plugin/index.ts` | CHANGE | WORKSPACE_BRIDGE_COMMANDS 新增 v7 命令 + connector:run；parseCommandInput 新增 v7 分支 |
| `plugin/workspace-ops-bridge.ts` | CHANGE/ADD | PackagedCliModule 扩展 auditStore 类型；动态导入 AppendOnlyAuditStore 并传入 router |
| `plugin/openclaw.plugin.json` | CHANGE | 描述追加 v7 ops surface 命令列表 |
| `src/cli/ops/manual-run-dispatcher.ts` | ADD | ManualRunDispatcher 工厂：runConnector / runWetProbe / runHeartbeatProbe |
| `src/cli/ops/ops-router.ts` | CHANGE/ADD | connector_test --wet triggerSource 修正为 "manual_run"；新增 connector:run 路由 |
| `README.md` | ADD | Mind/Body Alignment (v7) 对照表 |
| `AGENTS.md` | CHANGE/ADD | Bootstrap Recovery (DR-034) 强化铁律 + 恢复路径摘要表 + 路径说明 |
| `reports/v6-regression-gate-v7.md` | ADD | v6 回归测试门禁报告 |
| `tests/integration/plugin/plugin-registration.test.ts` | ADD | T-ROS.C.2 集成测试 |
| `tests/unit/ops/manual-run-dispatcher.test.ts` | ADD | T-ROS.C.3 单元测试 |

---

## 2. 测试统计

| 测试文件 | 数量 | 结果 |
|---|---|---|
| `tests/integration/plugin/plugin-registration.test.ts` | 12 | 12 PASS |
| `tests/unit/ops/manual-run-dispatcher.test.ts` | 6 | 6 PASS |
| 全量回归 (`dist/tests/**/*.test.js`) | 1128 | 1119 PASS / 9 FAIL (均为 pre-existing) |

---

## 3. 失败分析

全部 9 项失败均为 **pre-existing**，非 Wave 68 引入：

| # | 失败测试 | 根因 | 引入波次 |
|---|---|---|---|
| 1 | `T2.2.3 bridge full-runtime heartbeat wires connectorExecutor` | bridge connector action 未完全接线 | Wave 56+ |
| 2 | `T1.2.5-B cycle:recent aggregates multiple families` | audit genesis hash 未 seed | Wave 63-64 |
| 3 | `T1.2.7-B audit returns summary entries` | 同上 | Wave 63-64 |
| 4 | `INT-S3 source-backed draft → delivery failed` | audit hash chain broken | Wave 63-64 |
| 5 | `INT-S3 dropped_by_host_policy` | 同上 | Wave 63-64 |
| 6 | `schema-migration integration (v7-001)` 3 subtests | v7 schema 漂移 | v7 初始 |
| 7 | `resolveCapability unknown capability throws` | 旧 registry 行为变更 | Wave 46+ |

---

## 4. 代码审查发现

### Lens 1: 契约闭合
- `ManualRunDispatcher.runConnector` 严格执行 `triggerSource: "manual_run"` 和 `affectsHeartbeatCadence: false`
- `ExperienceWriter.recordExperience` 在成功和失败路径均被调用，不遗漏
- `connector:run` 命令在 ops-router 中 deps 缺失时返回 `MANUAL_RUN_DEPS_UNAVAILABLE`，降级语义正确

### Lens 2: 类型安全
- `RuntimeOpsEnvelope` 的 `surfaceMode` 使用 `"cli"` 而非 `"workspace_full_runtime"`，与类型定义兼容
- `ManualRunDispatcherDeps` 中 `registryV7: CapabilityContractRegistryV7` 正确传递

### Lens 3: 可维护性
- `manual-run-dispatcher.ts` 头注释完整说明核心逻辑、依赖关系、边界场景、测试覆盖
- 工厂函数 `createManualRunDispatcher` 不持有模块级状态，纯依赖注入

### Lens 4: 安全
- `AGENTS.md` Bootstrap Recovery 明确铁律：绝不输出密钥明文
- `connector:run` 不泄漏 credential / raw payload；redacted 体验记录

### Lens 5: 插件 VM 安全
- `workspace-ops-bridge.ts` 的 `auditStore` 动态导入不破坏 module-scope 无 DB 约束
- `plugin/index.ts` 的 `register()` 仍不在 module scope 触发 DB 初始化

---

## 5.  verdict

**PASS** — 0 Critical / 0 High / 0 Low

Wave 68 全部任务完成，无新回归，18 项新增测试全部通过，契约闭合。

---

*Generated by `/forge` Wave 68 settlement*
